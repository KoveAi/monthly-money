generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Expense {
  id             String    @id @default(cuid())
  description    String
  amount         Float
  amountPaid     Float     @default(0)
  // Unpaid balance rolled in from the previous month.
  broughtForward Float     @default(0)
  // Whether an unpaid balance on this line rolls into next month. Null means
  // "decide automatically" — recurring bills carry, one-offs and pay-to-use
  // subscriptions do not. Set true or false from the edit dialog to overrule that.
  carriesOver    Boolean?
  category       String
  dueDate        DateTime
  isRecurring    Boolean   @default(false)
  frequency      String    @default("monthly")
  status         String?
  paymentDate    DateTime?
  paymentType    String?
  notes          String?
  remainingName  String?
  remainingType  String?
  monthKey       String
  sortOrder      Int       @default(0)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
}
